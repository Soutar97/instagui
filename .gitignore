# dependencies
node_modules/

# build output
dist/

# secrets / env — never commit
.env
.env.*
*.pem
*.key

# instagui runtime + debug artifacts
.instagui/
instagui-debug-*.json

# logs / misc
*.log
*.bak

# local tooling — Claude Code config/skills, the BMAD authoring framework install,
# and the bmad-loop orchestrator state (not part of instagui; keep out of the public repo)
.claude/
_bmad/
.bmad-loop/

# local planning docs — kept out of the public repo (PRD, architecture, epics, resume note,
# product brief). Remove these lines if you ever want them tracked (e.g. a private repo).
_bmad-output/
product-brief.md

# BMAD document-project / superpowers planning + scan output — local context only, not
# instagui product docs. (docs/demo.gif and docs/*roadmap*.md are intentionally NOT ignored.)
docs/superpowers/
docs/api-contracts.md
docs/architecture.md
docs/data-models.md
docs/development-guide.md
docs/index.md
docs/project-overview.md
docs/project-scan-report.json
docs/source-tree-analysis.md

# personal launch-prep notes (kept local). docs/demo.gif IS meant to be committed.
docs/show-hn-draft.md
